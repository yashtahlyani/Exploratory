# Core computer-vision and ML
opencv-contrib-python>=4.8.0   # Haar cascade + LBPH face recognizer

# Image handling
Pillow>=10.0.0
numpy>=1.24.0

# Data / attendance records
pandas>=2.0.0
openpyxl>=3.1.0                # Excel export (pandas xlsx backend)

# Analytics charts
matplotlib>=3.7.0              # Daily trend + per-student attendance charts (TkAgg)
